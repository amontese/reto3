import 'package:flutter/material.dart';

class PaginaAndroid extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("ANDROID"),
    );
  }
}
